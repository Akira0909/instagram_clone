<% if user_signed_in? %>
  <div class="row">
    <div class="col-md-8">
      <h3>フィード</h3>
      <%= render 'shared/feed' %>
    </div>
    <aside class="col-md-4">
      <section class="user_info">
        <%= render 'shared/user_info' %>
      </section>
      <section class="stats">
        <%= render 'shared/stats' %>
      </section>
    </aside>
  </div>
<% else %>
  <h1>Instagram Clone</h1>
  <p>This is Instagram Clone application.</p>
<% end %>