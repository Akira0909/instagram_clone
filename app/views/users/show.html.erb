<% provide(:title, @user.name) %>
<div class="row">
  <aside>
    <section class="user_info">
      <h1>
      	<img class="user_image" src="<%= "/user_images/#{@user.image_name}" %>">
        <%= @user.name %>
      </h1>
    </section>
  </aside>
</div>